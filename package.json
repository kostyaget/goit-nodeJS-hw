{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index",
    "start:dev": "nodemon index"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "cli": "^1.0.1",
    "commander": "^9.4.0",
    "uuid": "^8.3.2"
  }
}
